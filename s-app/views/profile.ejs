<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile - Secure App</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="security-banner">SECURE VERSION</div>
    
    <div class="container">
        <div class="dashboard-header">
            <h1>User Profile</h1>
            <div class="header-actions">
                <div class="user-info">
                    Logged in as: <strong><%= user.username %></strong> (<%= user.role %>)
                </div>
                <a href="/dashboard" class="profile-link">Back to Dashboard</a>
            </div>
        </div>
        
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        
        <% if (profile) { %>
            <div class="profile-container">
                <div class="profile-section">
                    <h2>Account Information</h2>
                    <div class="profile-info">
                        <div class="info-row">
                            <span class="info-label">Username:</span>
                            <span class="info-value"><%= profile.username %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Role:</span>
                            <span class="info-value"><%= profile.role %></span>
                        </div>
                    </div>
                </div>
                
                <div class="profile-section">
                    <h2>Sensitive Note</h2>
                    <div class="note-container">
                        <% if (profile.sensitive_note) { %>
                            <div class="note-content">
                                <p><%= profile.sensitive_note %></p>
                            </div>
                        <% } else { %>
                            <div class="empty-state">
                                <p>No sensitive note found.</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        <% } %>
        
        <div class="text-center mt-24">
            <a href="/dashboard" class="link-secondary">Back to Dashboard</a>
        </div>
        
        <div class="text-center mt-24">
            <a href="/" class="logout-link">Logout</a>
        </div>
    </div>
</body>
</html>

